# 液压压力与推力代码说明手册

## 1. 工程位置

工程根目录：`E:\单片机\JH\JH_Pressure to thrust`

新增功能代码在 `Task` 文件夹：

| 文件 | 作用 |
| --- | --- |
| `pressure_measure.c/.h` | ADC1 十路采样、VREFINT、信号电压和压力计算。 |
| `thrust_calculator.c/.h` | 油缸面积、N、kg、吨推力换算。 |
| `screen_protocol.c/.h` | USART1 DMA 收发和显示屏协议配置。 |
| `app_control.c/.h` | 周期调度、缸径状态、单位状态、推力刷新。 |

Keil 工程 `MDK-ARM/JH_Pressure to thrust.uvprojx` 已将这四个 `.c` 文件加入 `Task` 分组。

## 2. 硬件对应关系

- MCU：STM32F103C8T6，72MHz。
- ADC：ADC1 的 IN0-IN9，即 PA0-PA7、PB0、PB1。
- 传感器：4-20mA 对应 0-30MPa。
- 采样电阻：150R，故 4mA 为 0.6V，20mA 为 3.0V。
- 模拟缓冲：OPA333 电压跟随器输出后分给十个 ADC 引脚。
- 串口屏：USART1，PA9 TX、PA10 RX、115200、DMA。
- VOFA：原有 USART3 代码未修改。

十个 ADC 引脚接的是同一个模拟信号，并不是十个独立传感器。程序每次采集十个码值，排序后去掉最低两个和最高两个，取中间六个平均值。这样可降低单路 ADC 瞬时异常对结果的影响，但不能代替硬件故障诊断。

## 3. 程序运行流程

```text
传感器 4-20mA
  -> 150R 采样电阻，得到 0.6-3.0V
  -> OPA333 电压跟随器
  -> ADC1 IN0-IN9 + 内部 VREFINT
  -> 压力 pressure_mpa
  -> 压力 + cylinder_d_mm
  -> 力 force_newton
  -> 按当前单位得到 thrust_value
  -> USART1 DMA 发送给显示屏
```

`main.c` 完成 CubeMX 外设初始化后调用 `AppControl_Init()`。主循环持续调用 `AppControl_Process()`。

`AppControl_Process()` 每 100ms 启动或读取一次 ADC 扫描结果；获得有效压力后，计算推力并尝试刷新显示屏。显示协议为空时，测量和计算仍会继续，但不会向屏幕发送任何内容。

## 4. 压力如何计算

ADC 的 12 位满量程是 4095。程序读取 VREFINT 后计算实际 ADC 参考电压：

```text
VDDA = 1.200 * 4095 / VREFINT_ADC码值
信号电压 = ADC平均码值 / 4095 * VDDA
压力(MPa) = (信号电压 - 0.600) / 0.080
```

最终压力限制为 `0.0-30.0MPa`，内部保留浮点精度；发送显示屏时由协议格式 `%.1f` 控制为一位小数。

`1.200V` 是 STM32F103 VREFINT 的标称值。若需要更高绝对精度，应使用万用表测量已知电流下的实际压力，再校准 `Task/pressure_measure.c` 中的 `PRESSURE_VREFINT_CALIBRATION_VOLTAGE`，或进一步增加零点/满量程软件校准系数。传感器本身的 ±0.5%FS 精度和温漂仍会影响最终结果。

## 5. 推力如何计算

```text
缸径米值 d_m = cylinder_d_mm / 1000
活塞面积 A = PI * d_m * d_m / 4
力 F(N) = pressure_mpa * 1,000,000 * A
F(kg) = F(N) / 9.80665
F(吨) = F(kg) / 1000
```

单位切换顺序为：`kg -> 吨 -> N -> kg`。

默认缸径为 `100.0mm`，允许设置范围为 `1.0-1000.0mm`。这两个值位于 `Task/app_control.c`：

```c
#define APP_DEFAULT_CYLINDER_DIAMETER_MM  (100.0f)
#define APP_MIN_CYLINDER_DIAMETER_MM      (1.0f)
#define APP_MAX_CYLINDER_DIAMETER_MM      (1000.0f)
```

## 6. 主要函数

| 函数 | 用途 |
| --- | --- |
| `AppControl_Init()` | 初始化默认缸径、默认单位、ADC 测量和串口屏接收 DMA。 |
| `AppControl_Process()` | 主循环调用的周期任务。 |
| `AppControl_SetCylinderDiameter(float)` | 设置合法缸径，返回 `true/false`。 |
| `PressureMeasure_Init()` | 校准 ADC1，清空测量状态。 |
| `PressureMeasure_Process()` | 启动 ADC DMA 或处理已完成的扫描；有新有效压力时返回 `true`。 |
| `PressureMeasure_GetMpa()` | 获得最近一次有效液压压力，单位 MPa。 |
| `PressureMeasure_GetSignalVoltage()` | 获得最近一次有效传感器电压。 |
| `PressureMeasure_GetVdda()` | 获得 VREFINT 推算的 VDDA。 |
| `Thrust_CalculateNewton(float, float)` | 由 MPa 与 mm 计算牛顿值。 |
| `Thrust_Convert(float, ThrustUnit)` | 将牛顿值转换为 kg、吨或 N。 |
| `ScreenProtocol_Init()` | 启动 USART1 的接收至空闲 DMA。 |
| `ScreenProtocol_SendMeasurements(float, float, ThrustUnit)` | 按配置格式发送压力、推力和单位。 |

## 7. 你后续必须填写的位置

打开 `Task/screen_protocol.c`，最顶部有以下中文注释框：

```c
/* ==================== 用户后续填写位置：显示屏通信协议 ==================== */
#define SCREEN_TX_MEASUREMENTS_FORMAT    ""
#define SCREEN_RX_DIAMETER_PREFIX        ""
#define SCREEN_RX_UNIT_COMMAND           ""
```

这里是唯一需要填写显示屏协议的地方。当前为空是安全状态：不会误发指令，也不会把任何串口数据当作有效缸径或按钮命令。

### 7.1 STM32 向屏幕发送变量

将 `SCREEN_TX_MEASUREMENTS_FORMAT` 填为屏幕实际需要的格式。该格式必须按顺序接收三个参数：

1. 压力值，float。
2. 当前单位下的推力值，float。
3. 单位文本，`kg`、`吨` 或 `N`。

例如，如果屏幕协议采用 ASCII 且三个变量一次发送，可写成：

```c
#define SCREEN_TX_MEASUREMENTS_FORMAT \
    "pressure=%.1f;Thrust=%.1f;tuili=%s\\r\\n"
```

这只是格式示例。必须以你的淘晶驰屏幕文档或串口抓包为准，特别是变量赋值语法、帧尾和字符串引号。若使用 Nextion 类三字节帧尾，需要写为 `\xFF\xFF\xFF`。

变量名 `pressure`、`Thrust`、`cylinder_d_mm`、`tuili` 目前没有被硬编码，正是为了让你按实际屏幕工程自行填写。

### 7.2 屏幕向 STM32 发送缸径

把 `SCREEN_RX_DIAMETER_PREFIX` 填成屏幕发送的缸径命令前缀。例如屏幕发：

```text
cylinder_d_mm=80.0
```

则设置为：

```c
#define SCREEN_RX_DIAMETER_PREFIX "cylinder_d_mm="
```

程序会读取后面的十进制浮点数，并在 `1.0-1000.0mm` 范围内更新缸径。超范围或格式错误的数据会被丢弃。

### 7.3 屏幕向 STM32 发送单位按钮命令

若单位按钮发送固定 ASCII 命令，例如：

```text
unit_button
```

则设置：

```c
#define SCREEN_RX_UNIT_COMMAND "unit_button"
```

收到完全相同的一帧后，STM32 切换 `kg -> 吨 -> N`，下一次刷新时重新发送单位和推力。

## 8. 串口 DMA 工作方式

- USART1 接收使用 `HAL_UARTEx_ReceiveToIdle_DMA()`。
- 一帧数据停止一段时间后，UART IDLE 中断触发 `HAL_UARTEx_RxEventCallback()`。
- 回调函数只识别缸径命令和单位命令，然后立即重新开启接收 DMA。
- USART1 发送使用 `HAL_UART_Transmit_DMA()`，发送未完成时不覆盖发送缓冲区。
- 显示屏协议未配置时，`ScreenProtocol_SendMeasurements()` 返回 `SCREEN_PROTOCOL_NOT_CONFIGURED`，不会阻塞主循环。

## 9. 重新生成 CubeMX 代码的注意事项

当前 `Core/Src/adc.c` 已手动配置 ADC1 为 11 通道扫描和 DMA1 Channel1。不要直接重新生成 CubeMX 工程后立刻覆盖这些改动。

若需要在 CubeMX 中重新配置，请保持以下条件：

- 仅使用 ADC1 进行本功能采样。
- Regular Conversion 排名 1-10 为 IN0-IN9，排名 11 为 VREFINT。
- 采样时间全部为 `239.5 cycles`。
- Scan Conversion 开启，转换数为 11。
- ADC1 DMA 使用 DMA1 Channel1、Peripheral to Memory、Half Word、Normal 模式。
- DMA1 Channel1 中断开启。

## 10. 编译与上板检查

1. 在 Keil 打开 `MDK-ARM/JH_Pressure to thrust.uvprojx`。
2. 执行 `Rebuild`，确认没有 Error。
3. 烧录后先通过 VOFA 或调试器观察 `PressureMeasure_GetVdda()`、`PressureMeasure_GetSignalVoltage()` 和 `PressureMeasure_GetMpa()`。
4. 使用已知 4mA、12mA、20mA 电流分别确认约为 `0MPa`、`15MPa`、`30MPa`。
5. 填写屏幕协议配置区，再检查屏幕压力、推力、缸径设置和单位按钮。
